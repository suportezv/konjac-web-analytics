# Konjac Web Analytics: dependências Python da célula de análise.
# Instaladas por scripts/setup.sh no venv .venv/ da raiz do repo.
# Somente pip: o proxy do ambiente cloud bloqueia apt (403 em archive.ubuntu.com),
# mas pypi.org e files.pythonhosted.org estão no NO_PROXY e respondem direto.
#
# Versões fixadas a partir do que resolveu e importou de fato em 2026-08-27,
# Python 3.11.15 / Ubuntu 24.04. Ao subir uma versão, rode scripts/validate.sh.

# --- Google Cloud / BigQuery ---
google-cloud-bigquery==3.44.0
google-auth==2.57.0
db-dtypes==1.7.1

# --- GA4: Data API (relatórios) e Admin API (inventário de propriedades) ---
google-analytics-data==0.23.0
google-analytics-admin==0.30.1

# --- Google Ads ---
google-ads==31.4.0

# --- HTTP: Meta Graph API e Shopify Admin API ---
requests==2.34.2

# --- Recorte e manipulação de dados ---
pandas==3.0.5
pyarrow==25.0.1
python-dateutil==2.9.0.post0
tabulate==0.10.0
